# v2.9.0 工程审阅

- 结论：通过。
- `data-layout` 作为页面结构身份；`data-theme` 和 `data-density` 只影响阅读条件。
- 指针位置由 `requestAnimationFrame` 驱动并在组件卸载时取消，避免遗留动画帧。
- 路由转场继续由页面 key 触发，抽屉和表单状态不随页面切换清空。
- 通过类型检查、30 个单元测试、Vite 构建和 Tauri `--no-bundle` 构建。
- 本机未生成安装包，直接运行产物为 `src-tauri/target/release/memento.exe`。
