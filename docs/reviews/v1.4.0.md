# v1.4.0 发布后自我审阅

状态：候选版本；日期计算、真实日期展示和 Windows 构建已完成，待远程 PR/Release 权限补齐后正式发布。

## 本轮完成

- 余下列表展示接下来最多 5 个真实日期，而不是只展示一个数量。
- 日期条目同时显示月日和星期，超过 5 个时显示剩余数量。
- 多段余下自动按截止日期从近到远排序，优先看到最紧迫的目标。
- 修正“周末”计算：同时包含周六和周日。
- 明确截止日期边界：截止日当天是目标星期时计入，今天是截止日时不再产生未来日期。
- 增加周末、截止日当天和既有周五逻辑的单元测试。

## 已验证

- `npm run typecheck` 通过。
- `npm test -- --run` 通过，3 个测试文件、13 个测试全部通过。
- Playwright 真实交互验证：余下列表显示后续日期条目和剩余数量。
- `npm run tauri:build` 通过；`memento.exe` 启动 3 秒后进程保持运行且窗口响应正常。

## 本地安装包证据

- 文件：`src-tauri/target/release/bundle/nsis/Memento_1.4.0_x64-setup.exe`
- 体积：2.88 MB
- SHA-256：`7364FD8585058631C0D73E31C61975A4A6A8C2A76F3871CB0FC655918504EBCB`

## 仍然存在的不足

1. 日期列表最多展示 5 项，尚未提供独立的完整日期展开面板。
2. “周末”目前定义为周六和周日，尚未允许用户自定义周末组合。
3. 余下条目还没有编辑与删除入口，仍依赖新建流程。
4. 尚未完成干净 Windows 环境的安装、卸载和升级验证。
5. GitHub 当前令牌仍不能创建 PR 或 Release，只能推送功能分支。

## 风险等级

- P0：暂无。
- P1：安装/升级链路与远程发布尚未完成。
- P2：完整日期展开、自定义周末、余下条目编辑。

## 下一轮优先事项

- v1.5：集中处理键盘可达性、错误状态、性能和安装流程说明。
