# 3.1.0 视觉审阅

审阅日期：2026-08-28

## 结论

通过。河流漂流使用深墨实色阅读面、SVG 河道和 Canvas 低亮度水纹，正文仍为 DOM 文本，可复制、可搜索、可访问；水纹只随鼠标改变局部状态，不改变布局尺寸。

## 验证范围

- 此刻、时光、几度、我的四个页面均保持独立布局身份。
- 1280×720 实测 `scrollWidth <= innerWidth`。
- 漂流层完成截图验收；进入 390ms，使用 transform/opacity，不动画 width/height/top/left。
- 鼠标悬停、键盘焦点和窗口不可见时自动暂停。
- `prefers-reduced-motion: reduce` 下取消自动播放和水纹运动，仅保留即时切换。
- 高对比模式保留正文、来源、日期和操作，不依赖装饰性样片。
