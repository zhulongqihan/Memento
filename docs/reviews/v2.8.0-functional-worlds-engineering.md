# v2.8.0 工程审阅

- 结论：通过。
- 四页路由分别渲染独立 JSX 主结构，保留共享业务状态和路由外抽屉。
- 页面转场以路由 key 取消旧动画并进入最新目标；reduced-motion 关闭动画。
- 验收根节点布局标识为 `clock-stage`、`film-wall`、`measurement-instrument`、`registry-document`。
- 1280×720、1440×900、1920×1080 浏览器检查无横向溢出。
- 发布只构建 `memento.exe`，不生成 NSIS/MSI 安装包。

