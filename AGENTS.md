# 几度 · Memento 工程规则

## 产品原则

- 几度不是效率工具、Todo List 或普通日记，而是安静的人生时间记录产品。
- 所有功能必须强化“感知已经过去的时间、正在经历的时间、还剩下的时间”。
- 未经明确要求，不增加社交、AI、地图、天气、积分或复杂账户功能。
- 先保证记录、查看、计算和备份闭环，再增加表达层功能。

## 桌面范围

- 当前只支持 Windows PC，主设计尺寸为 1440×900。
- 1280–1920px 为验收范围，不做移动端布局。
- 最终交付物必须是可以双击安装的 `.exe`。

## 视觉规则

- 使用 Editorial / Personal Archive / Quiet 的设计语言。
- 禁止蓝紫渐变、玻璃拟态、Bento Dashboard、大面积阴影和过度圆角。
- 页面优先使用排版、细线、照片和留白，不用大量重复卡片。
- 所有交互必须考虑键盘焦点、错误状态和 reduced motion。

## 发布规则

- 每个版本在独立分支完成，通过测试后提交 Pull Request。
- 每次发布必须生成 Windows 安装包、校验值和发布说明。
- 每次发布后必须进行产品、视觉、数据安全和工程审阅，并记录到 `docs/reviews/`。
- P0/P1 问题必须在后续版本前处理，不允许用发布说明掩盖数据损坏。

